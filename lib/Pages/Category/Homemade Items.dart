import 'package:flutter/material.dart';
import '../../Custom_widgets/category_item_each_custom.dart';

class HomeMadeItemsPage extends StatelessWidget {
  const HomeMadeItemsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const CategoryItemsPage(categoryName: 'হস্তশিল্প (Handmade Items)');
  }
}
