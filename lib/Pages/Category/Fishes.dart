import 'package:flutter/material.dart';
import '../../Custom_widgets/category_item_each_custom.dart';

class FishesPage extends StatelessWidget {
  const FishesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const CategoryItemsPage(categoryName:     'মাছ (Fishes)');
  }
}
